import { describe, it, expect } from "vitest";
import { closePositionTool } from "./closePositionTool";

describe("closePositionTool", () => {
	it("exports expected members", () => {
		expect(closePositionTool).toBeDefined();
	});
});
